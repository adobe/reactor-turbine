{
  "name": "@reactor/turbine",
  "version": "5.0.0",
  "description": "DTM engine that contains the brains responsible for running rules on a client's website as well as delegating logic to extensions.",
  "scripts": {
    "test": "gulp test",
    "ci-job": "xvfb-run gulp test --ci --browser all && gulp lint && npm run plato",
    "plato": "plato -r -d platoReport src"
  },
  "author": {
    "name": "DTM",
    "email": "DTM-Engineering@adobe.com"
  },
  "repository": {
    "type": "git",
    "url": "git@git.corp.adobe.com:reactor/turbine.git"
  },
  "publishConfig": {
    "registry": "https://artifactory.corp.adobe.com/artifactory/api/npm/npm-mcps-release-local"
  },
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-gzip": "^1.2.0",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^1.5.3",
    "is-plain-object": "^2.0.1",
    "native-promise-only": "^0.8.1",
    "strict-loader": "^1.0.0",
    "webpack": "^1.12.14",
    "webpack-stream": "^3.1.0"
  },
  "devDependencies": {
    "@reactor/turbine-gulp-testrunner": "^3.0.0"
  }
}
